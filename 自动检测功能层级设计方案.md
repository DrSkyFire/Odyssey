# 自动检测功能层级设计方案

## 📋 需求分析

用户希望实现一个**层级化的自动检测调整模式**：
- 进入自动检测模式后，可以选择不同的参数调整子模式
- 每个子模式内可以调整上下限
- 支持多层级退出

## 🔍 当前实现分析

### 现有按键分配（自动测试模式）
```verilog
// 当前实现：auto_test_enable = 1 时
assign btn_freq_up    = btn_0_pulse & auto_test_enable;  // 频率增加
assign btn_freq_dn    = btn_1_pulse & auto_test_enable;  // 频率减少
assign btn_amp_up     = btn_2_pulse & auto_test_enable;  // 幅度增加
assign btn_amp_dn     = btn_3_pulse & auto_test_enable;  // 幅度减少
assign btn_duty_up    = btn_4_pulse & auto_test_enable;  // 占空比增加
assign btn_thd_adjust = btn_6_pulse & auto_test_enable;  // THD调整
```

### 现有auto_test.v接口
```verilog
input  wire btn_freq_up,    // 频率上限增加
input  wire btn_freq_dn,    // 频率上限减少
input  wire btn_amp_up,     // 幅度上限增加
input  wire btn_amp_dn,     // 幅度下限减少
input  wire btn_duty_up,    // 占空比容差增加
input  wire btn_thd_adjust, // THD阈值调整（循环）
```

### ⚠️ 问题识别

1. **当前设计问题**：
   - 只能调整单向（上限或下限）
   - 没有下限/上限独立控制
   - THD只支持循环，不支持精细调整

2. **用户方案冲突分析**：

| 按键 | 用户方案功能 | 当前已有功能 | 冲突情况 |
|------|------------|------------|---------|
| button[0] | 进入频率调整模式 | btn_mode（默认模式） | ❌ auto_test_enable=1时，btn_mode已被禁用，可用 |
| button[1] | 进入幅度调整模式 | btn_weak_sig_enable（默认） | ❌ auto_test_enable=1时，weak_sig已被禁用，可用 |
| button[2] | 进入占空比调整模式 | btn_stop（默认） | ❌ auto_test_enable=1时，btn_stop已被禁用，可用 |
| button[3] | 进入THD调整模式 | btn_ch1_toggle（默认） | ❌ auto_test_enable=1时，ch1已被禁用，可用 |
| button[7] | 退出当前模式 | btn_test_mode + adc_otr_clear | ⚠️ 冲突！需要处理优先级 |

**结论**：button[0-3]在auto_test_enable=1时可用，但button[7]有冲突。

## 🎯 改进方案设计

### 方案A：状态机层级设计（推荐）

引入两级状态机：
1. **一级状态**: 自动测试模式开关（auto_test_enable）
2. **二级状态**: 参数选择模式（param_adjust_mode）

#### 状态定义
```verilog
// 参数调整模式编码
localparam ADJUST_IDLE   = 3'd0;  // 空闲（参数选择界面）
localparam ADJUST_FREQ   = 3'd1;  // 频率调整
localparam ADJUST_AMP    = 3'd2;  // 幅度调整
localparam ADJUST_DUTY   = 3'd3;  // 占空比调整
localparam ADJUST_THD    = 3'd4;  // THD调整

reg [2:0] param_adjust_mode;
```

#### 按键映射逻辑

**层级1：自动测试模式（auto_test_enable = 1）**
```verilog
// 参数选择界面（param_adjust_mode = ADJUST_IDLE）
if (auto_test_enable && param_adjust_mode == ADJUST_IDLE) begin
    button[0] -> 进入频率调整模式
    button[1] -> 进入幅度调整模式
    button[2] -> 进入占空比调整模式
    button[3] -> 进入THD调整模式
    button[5] -> 退出自动测试模式（toggle auto_test_enable）
    button[7] -> 退出自动测试模式（额外退出键）
end
```

**层级2：参数调整界面（param_adjust_mode != IDLE）**
```verilog
// 在任意参数调整模式内
if (auto_test_enable && param_adjust_mode != ADJUST_IDLE) begin
    button[0] -> 下限减少
    button[1] -> 下限增加
    button[2] -> 上限减少
    button[3] -> 上限增加
    button[4] -> 步进切换（粗调/细调）
    button[6] -> 恢复默认值
    button[7] -> 退出到参数选择界面
end
```

### 方案B：简化单层级设计（备选）

不引入二级状态，直接在按键上做功能切换：

```verilog
// 长按button[5]进入自动测试模式
// 短按button[0-3]直接调整对应参数的上限
// 长按button[0-3]调整对应参数的下限
```

**缺点**：需要实现长按检测，增加复杂度。

## 📊 冲突分析与解决

### button[7]的冲突处理

**当前用途**：
1. `btn_test_mode` - 切换测试模式（信号源开关）
2. `adc_otr_clear` - 清除ADC溢出标志

**新增需求**：
3. 退出参数调整子模式（层级2）
4. 退出自动测试模式（层级1）

**解决方案**：优先级控制
```verilog
// 优先级: 参数调整 > 自动测试 > 测试模式/溢出清除
if (auto_test_enable && param_adjust_mode != ADJUST_IDLE)
    // button[7] -> 退出到参数选择界面
else if (auto_test_enable && param_adjust_mode == ADJUST_IDLE)
    // button[7] -> 退出自动测试模式
else
    // button[7] -> 测试模式切换 + 溢出清除
```

## 🛠️ 实现建议

### 信号定义（signal_analyzer_top.v）
```verilog
// 新增信号
reg [2:0]  param_adjust_mode;   // 参数调整模式
reg [1:0]  adjust_step_mode;    // 步进模式（0=细调, 1=中调, 2=粗调）

// 新增按键信号（内部使用）
wire btn_enter_freq_adjust;     // 进入频率调整
wire btn_enter_amp_adjust;      // 进入幅度调整
wire btn_enter_duty_adjust;     // 进入占空比调整
wire btn_enter_thd_adjust;      // 进入THD调整
wire btn_exit_param_adjust;     // 退出参数调整模式
wire btn_limit_dn_dn;           // 下限减少
wire btn_limit_dn_up;           // 下限增加
wire btn_limit_up_dn;           // 上限减少
wire btn_limit_up_up;           // 上限增加
wire btn_step_toggle;           // 步进切换
wire btn_reset_default;         // 恢复默认
```

### 状态机逻辑
```verilog
// 参数调整模式状态机
always @(posedge clk_100m or negedge rst_n) begin
    if (!rst_n)
        param_adjust_mode <= ADJUST_IDLE;
    else if (!auto_test_enable)
        param_adjust_mode <= ADJUST_IDLE;  // 退出自动测试时复位
    else begin
        case (param_adjust_mode)
            ADJUST_IDLE: begin
                if (btn_enter_freq_adjust)
                    param_adjust_mode <= ADJUST_FREQ;
                else if (btn_enter_amp_adjust)
                    param_adjust_mode <= ADJUST_AMP;
                else if (btn_enter_duty_adjust)
                    param_adjust_mode <= ADJUST_DUTY;
                else if (btn_enter_thd_adjust)
                    param_adjust_mode <= ADJUST_THD;
            end
            
            default: begin  // 任意调整模式
                if (btn_exit_param_adjust)
                    param_adjust_mode <= ADJUST_IDLE;
            end
        endcase
    end
end
```

### auto_test.v接口扩展
```verilog
// 新增接口
input  wire [2:0]   adjust_mode,        // 当前调整模式
input  wire         btn_limit_dn_dn,    // 下限减少
input  wire         btn_limit_dn_up,    // 下限增加  
input  wire         btn_limit_up_dn,    // 上限减少
input  wire         btn_limit_up_up,    // 上限增加
input  wire         btn_reset_default,  // 恢复默认值
input  wire [1:0]   step_mode,          // 步进模式

// 需要为每个参数维护上下限
reg [15:0] freq_min, freq_max;
reg [15:0] amp_min, amp_max;
reg [15:0] duty_min, duty_max;
reg [15:0] thd_max;  // THD只有上限
```

## ⚡ 优化建议

### 1. 参数配置（已确认）

**步进值配置**：
- 频率：1Hz（细调）/ 100Hz（中调）/ 100kHz（粗调）
- 幅度：1mV（细调）/ 100mV（中调）/ 1V（粗调）
- 占空比：0.1%（细调）/ 1%（中调）/ 10%（粗调）
- THD：0.1%（细调）/ 1%（中调）/ 10%（粗调）

**默认阈值**：
- 频率：100kHz（目标值，需要上下限容差）
- 幅度：3V（目标值，需要上下限容差）
- 占空比：60%（目标值，需要容差）
- THD：60%（最大值）

**按键分配变更**：
- ✅ button[7]完全分配给自动测试功能（不再用于测试模式切换）
- ✅ 移除内部测试信号功能
- ✅ test_mode相关逻辑可以简化或移除

**HDMI显示**：
- 位置：屏幕右下角
- 内容：当前调整状态 + 参数值 + 按键提示
- 触发：进入自动测试模式后显示

### 2. LED指示优化
```verilog
// LED[7:6] - 模式指示
// 00: 正常模式
// 01: 自动测试-参数选择
// 10: 自动测试-频率调整
// 11: 自动测试-其他参数调整

// LED[5:4] - 步进模式
// 00: 细调
// 01: 中调
// 10: 粗调

// LED[3:0] - 测试结果
```

### 2. HDMI显示提示
建议在屏幕上显示当前状态：
```
[自动测试模式]
当前调整：频率范围
  下限: 980 Hz    [0]减少 [1]增加
  上限: 1020 Hz   [2]减少 [3]增加
  步进: 细调(1Hz) [4]切换
[6]恢复默认  [7]返回
```

### 3. 参数持久化
建议将调整后的阈值保存到寄存器，复位后恢复默认值。

## 📝 实现清单

### Phase 1: 基础架构（P0）
- [ ] 在signal_analyzer_top.v中添加param_adjust_mode状态机
- [ ] 实现按键映射逻辑（两层分发）
- [ ] 处理button[7]的优先级冲突
- [ ] 测试模式切换是否正常

### Phase 2: auto_test.v改造（P0）
- [ ] 为每个参数添加min/max寄存器
- [ ] 实现上下限独立调整逻辑
- [ ] 添加步进模式支持（细调/中调/粗调）
- [ ] 实现恢复默认值功能

### Phase 3: 用户体验（P1）
- [ ] LED状态指示
- [ ] HDMI界面提示
- [ ] 参数范围限制和保护
- [ ] 操作反馈（如参数到达极限时LED闪烁）

### Phase 4: 测试验证（P0）
- [ ] 功能测试（每个按键功能）
- [ ] 边界测试（参数极限值）
- [ ] 模式切换测试
- [ ] 长时间稳定性测试

## ⚠️ 风险评估

### 高风险
1. **button[7]优先级混乱**：可能导致误操作
   - 缓解：清晰的优先级文档 + LED状态指示
   
2. **状态机复杂度增加**：可能引入新bug
   - 缓解：充分的仿真测试

### 中风险
3. **用户操作流程复杂**：需要学习成本
   - 缓解：HDMI界面提示 + 用户手册

4. **参数配置丢失**：复位后需重新设置
   - 缓解：考虑添加参数保存功能（后期优化）

## 🎓 总结与建议

### 推荐方案：**方案A（状态机层级设计）**

**优点**：
✅ 清晰的层级结构
✅ 功能完整（上下限独立调整）
✅ 可扩展性强（后续可添加更多参数）
✅ 无按键冲突（通过状态隔离）

**需要注意**：
⚠️ button[7]优先级需要明确定义
⚠️ LED状态指示很重要（用户需要知道当前在哪一层）
⚠️ 需要更新用户文档

### 下一步行动
1. 确认方案是否符合预期
2. 我可以开始实现Phase 1（基础架构）
3. 需要确认步进模式的具体数值（细调/中调/粗调的步进大小）
4. 需要确认默认阈值范围

**你觉得这个设计方案如何？是否需要调整？**
